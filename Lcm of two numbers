# include<studio.h>
int main|()
{
int num1,num2,1cm,fact=1;
printf("enter2 numbers\n');
scanf("%d%d",&num1,&num2);
lcm=(num1>num2)? num1:num2;
while(fact)
{
if(lcm%num1==0 && lcm%num2==0)
 {
  printf("lcm of %d and %d is %d\n", num1,num2,lcm);
  }
  lcm++;
  }
